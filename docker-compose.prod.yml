services:
  backend:
    image: ghcr.io/trutoman/apalabrazos:latest
    container_name: apalabrazos-backend
    restart: unless-stopped
    ports:
      - "8080:8080"
    # Los secretos se inyectan en tiempo de deploy por el runner de GitHub Actions.
    # No se necesita .env ni Watchtower.
