# Registro de tareas — 2026-04-02 / 2026-04-04

## Objetivo del día
Dejar Apalabrazos listo para ejecutar en contenedor y avanzar hacia despliegue productivo en VPS.

## Tareas realizadas

1. Rama de trabajo
   - Se creó la rama `leandroPII` para trabajar en paralelo.

2. Dockerización backend
   - Se añadió `Dockerfile` multi-stage para Java 21 + Maven.
   - Se añadió `.dockerignore`.
   - Se verificó arranque del servidor en `:8080`.

3. Variables de entorno y runtime
   - Se validó que JWT es requerido para boot.
   - Se validó que Cosmos es requerido para operaciones de usuarios.
   - Se quitó hardcode de secretos del Dockerfile (mejor práctica).

4. Carga de preguntas a Cosmos
   - Se creó `CosmosQuestionSeeder` para cargar preguntas desde JSON.
   - Se soportó carga opcional al arranque con `SEED_QUESTIONS_ON_START=true`.
   - Se importó `questions2.json` desde rama `main` con éxito (81 upserts).

5. Backend auth/login
   - Se corrigió manejo de duplicados de email en login.
   - Se bloquea registro duplicado de email con `409`.

6. Producción / VPS
   - Se creó `docker-compose.prod.yml` con backend + watchtower.
   - Se creó workflow de GitHub Actions para publicar imagen a GHCR.
   - Se documentó despliegue en VPS en `docs/VPS_DEPLOY.md`.

## Resultado
- Backend contenedorizado y funcionando local.
- Flujo de despliegue productivo preparado para VPS.

## Pendiente inmediato
- Copiar archivos a la VPS.
- Configurar `.env` productivo con secretos vigentes.
- Login GHCR en la VPS.
- Levantar stack con compose.

## Estado actualizado (ejecutado)

- Docker instalado en la VPS `138.68.174.4`.
- Imagen transferida y levantada en producción con `docker compose`.
- Servicio activo en:
   - `http://138.68.174.4:8080/` (HTTP 200)
- Se validó arquitectura de imagen para VPS (`linux/amd64`) y se redeployó correctamente.
- Se detectó que la VPS corre Debian (trixie), no Ubuntu, y se ajustó instalación de Docker en consecuencia.

## Estado actualizado (2026-04-04)

- Reverse proxy Nginx configurado en VPS para enrutar tráfico al backend interno (`127.0.0.1:8080`).
- SSL de Let's Encrypt activado correctamente.
- Dominios funcionando con HTTPS:
   - `https://apalabrazos.app`
   - `https://www.apalabrazos.app`
- Redirección HTTP → HTTPS activa en ambos dominios.
- Certificado validado con SAN para `apalabrazos.app` y `www.apalabrazos.app`.

### Tecnologías y servicios usados (hoy)

- **Nginx** como reverse proxy en la VPS.
- **Certbot** (Let's Encrypt) para emisión e instalación del certificado SSL.
- **Name.com** para registro y gestión DNS del dominio.
   - Coste del dominio en este caso: **0 €** usando beneficio de **GitHub Student Developer Pack**.

### Runtime en VPS
- Container: `apalabrazos-backend`
- Restart policy: `unless-stopped`
- Puerto publicado: `8080:8080`

### Próximo paso recomendado
- Dejar `watchtower` sincronizando de forma controlada la imagen publicada desde la rama de trabajo (`leandroPII`) en GitHub.

## Siguiente paso acordado (próxima sesión)

Objetivo: asegurar auto-deploy predecible desde la rama `leandroPII` usando Watchtower + GHCR.

Plan de trabajo:
1. Definir estrategia de tags por rama en GitHub Actions:
   - `main` → `:latest`
   - `leandroPII` → `:leandroPII` (o equivalente estable por rama)
2. Ajustar `docker-compose.prod.yml` para que el backend use el tag de rama que queremos seguir en VPS.
3. Verificar login de GHCR en VPS (`read:packages`) para que Watchtower pueda hacer pull sin fallo.
4. Validar ciclo completo:
   - push en `leandroPII`
   - nueva imagen en GHCR
   - Watchtower detecta cambio
   - reinicio automático de `apalabrazos-backend`
5. Documentar procedimiento de rollback rápido al tag anterior si algo falla.

Checklist para la sesión:
- [ ] Confirmar tag final que seguirá producción (`latest` vs `leandroPII`).
- [ ] Aplicar cambios en workflow y compose.
- [ ] Probar update automático end-to-end.
- [ ] Dejar rollback documentado.
